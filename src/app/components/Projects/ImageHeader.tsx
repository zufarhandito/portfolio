import React from 'react'
import Image from 'next/image';

type Props = {}

export default function ImageHeader({ }: Props) {
    return (
        <Image alt="project_image" width={50} height={50} src="">Image</Image>
    )
}